---
wave: 2
depends_on: [05-01]
files_modified:
  - backend/app/services/movie.py
  - backend/app/routes/user.py
requirements_addressed: [DISC-01, DISC-02, DISC-03]
autonomous: true
---

# Phase 5-2: Add Search and Category Filter to Backend API

## Objective

Extend the GET /movies endpoint to support search (q) and category query parameters.

## Tasks

### Task 1: Add Search Method to Movie Service

<read_first>
- backend/app/services/movie.py
- backend/app/models/movie.py
</read_first>

<action>
Add get_published_filtered method:
- Accepts search and category parameters
- Uses ilike for case-insensitive title search
- Filters by category when provided
- Returns filtered published movies
</action>

<acceptance_criteria>
- movie_service has get_published_filtered method
- Method uses ilike for search
- Method filters by category
</acceptance_criteria>

### Task 2: Add Get Categories Method

<read_first>
- backend/app/services/movie.py
</read_first>

<action>
Add get_categories method:
- Returns distinct categories from published movies
- Filters out null categories
</action>

<acceptance_criteria>
- movie_service has get_categories method
- Returns list of category strings
</acceptance_criteria>

### Task 3: Update User Routes

<read_first>
- backend/app/routes/user.py
</read_first>

<action>
Update list_movies endpoint:
- Add q: Optional[str] = None parameter
- Add category: Optional[str] = None parameter
- Call get_published_filtered with these params
</action>

<acceptance_criteria>
- GET /movies accepts q parameter
- GET /movies accepts category parameter
</acceptance_criteria>

### Task 4: Add Categories Endpoint

<action>
Add GET /categories endpoint:
- Returns list of available categories
- Requires authentication
</action>

<acceptance_criteria>
- GET /categories endpoint exists
- Returns category list
</acceptance_criteria>

## Must-Haves

- [ ] GET /movies accepts q parameter
- [ ] GET /movies accepts category parameter
- [ ] GET /categories returns available categories
