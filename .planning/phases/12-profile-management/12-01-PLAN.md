---
wave: 1
depends_on: []
files_modified:
  - backend/app/models/user.py
  - backend/app/schemas/user.py
  - backend/app/routes/user.py
  - frontend/src/routes/Profile.tsx
  - frontend/src/App.tsx
requirements_addressed:
  - PROF-01
  - PROF-02
  - PROF-03
autonomous: true
---

# Phase 12-01: Profile Management

<objective>
Implement user profile viewing, display name updates, and password change functionality.
</objective>

<read_first>
- backend/app/models/user.py — User model
- backend/app/routes/auth.py — Current auth endpoints
- backend/app/routes/user.py — User routes
- frontend/src/App.tsx — Route definitions
</read_first>

<tasks>

<task id="1" type="execute">
<name>Add display_name field to User model</name>
<action>
Update `backend/app/models/user.py`:
- Add `display_name: Mapped[str | None]` — nullable, optional display name (max 100 chars)
- Keep existing fields unchanged
</action>
<acceptance_criteria>
- User model has `display_name` field (nullable String)
</acceptance_criteria>
</task>

<task id="2" type="execute">
<name>Add profile update schemas</name>
<action>
Update `backend/app/schemas/user.py`:
- Add `ProfileUpdate` schema with `display_name: str | None`
- Add `PasswordChange` schema with `current_password: str` and `new_password: str`
- Add `ProfileResponse` extending UserResponse with display_name
</action>
<acceptance_criteria>
- Schemas exist for ProfileUpdate, PasswordChange, ProfileResponse
</acceptance_criteria>
</task>

<task id="3" type="execute">
<name>Add profile endpoints</name>
<action>
Create/update `backend/app/routes/user.py`:
- `GET /users/me` — Return current user profile
- `PUT /users/me` — Update profile (display_name)
- `POST /users/me/password` — Change password (requires current_password verification)
</action>
<acceptance_criteria>
- GET /users/me returns user profile
- PUT /users/me updates display_name
- POST /users/me/password changes password with verification
</acceptance_criteria>
</task>

<task id="4" type="execute">
<name>Create Profile page frontend</name>
<action>
Create `frontend/src/routes/Profile.tsx`:
- Display current user info (email, display_name)
- Form to update display_name
- Form to change password (current + new + confirm)
- Use same styling patterns as other pages
</action>
<acceptance_criteria>
- Profile.tsx exists with profile display and update forms
</acceptance_criteria>
</task>

<task id="5" type="execute">
<name>Add Profile route to App.tsx</name>
<action>
Update `frontend/src/App.tsx`:
- Add route for `/profile` → Profile component (protected)
</action>
<acceptance_criteria>
- /profile route exists and is protected
</acceptance_criteria>
</task>

</tasks>

<verification>
- Backend builds without errors
- Frontend builds without errors
- Profile page accessible when logged in
- Display name can be updated
- Password can be changed with current password verification
</verification>

<must_haves>
- User can view their profile
- User can update display name
- User can change password when logged in
</must_haves>
