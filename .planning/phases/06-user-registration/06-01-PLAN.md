---
wave: 1
depends_on: []
files_modified:
  - backend/app/routes/auth.py
  - frontend/src/routes/Register.tsx
  - frontend/src/auth/AuthContext.tsx
  - frontend/src/api/auth.ts
  - frontend/src/App.tsx
  - frontend/src/routes/Login.tsx
requirements_addressed: [ACC-01, ACC-02, ACC-03]
autonomous: true
---

# Phase 6-1: User Registration

## Objective

Add public registration endpoint and UI to allow new users to create accounts.

## Tasks

### Task 1: Add Registration Endpoint

<read_first>
- backend/app/routes/auth.py
- backend/app/services/user.py
- backend/app/schemas/user.py
</read_first>

<action>
Add POST /auth/register endpoint:
- Accept email and password (UserCreate schema)
- Validate password minimum 8 characters
- Check email uniqueness
- Create user with default USER role
- Return access token (auto-login)
- Set refresh token cookie
</action>

<acceptance_criteria>
- POST /auth/register endpoint exists
- Validates password length
- Returns 400 for duplicate email
- Returns access token on success
</acceptance_criteria>

### Task 2: Add Password Validation

<read_first>
- backend/app/schemas/user.py
</read_first>

<action>
Add Pydantic validator to UserCreate:
- Validate password minimum 8 characters
- Raise ValueError with clear message if too short
</action>

<acceptance_criteria>
- UserCreate.password validated for length
- Clear error message returned
</acceptance_criteria>

### Task 3: Add Register API Function

<read_first>
- frontend/src/api/auth.ts
</read_first>

<action>
Add register function:
- POST to /auth/register
- Send email and password
- Return LoginResponse (access_token)
</action>

<acceptance_criteria>
- register function exists
- Returns LoginResponse type
</acceptance_criteria>

### Task 4: Add Register to AuthContext

<read_first>
- frontend/src/auth/AuthContext.tsx
</read_first>

<action>
Add register function to AuthContext:
- Call register API
- Store token in localStorage
- Update user state
- Same pattern as login
</action>

<acceptance_criteria>
- register function in AuthContext
- Token stored after registration
- User state updated
</acceptance_criteria>

### Task 5: Create Register Component

<read_first>
- frontend/src/routes/Login.tsx
</read_first>

<action>
Create Register.tsx:
- Copy Login.tsx structure
- Change title to "Register"
- Call register() instead of login()
- Redirect to /movies on success
- Add link back to login
</action>

<acceptance_criteria>
- Register component exists
- Form has email and password fields
- Calls register on submit
- Redirects to /movies
</acceptance_criteria>

### Task 6: Update Login Component

<read_first>
- frontend/src/routes/Login.tsx
</read_first>

<action>
Add registration link to Login.tsx:
- Add text "Don't have an account? Register"
- Link to /register
- Remove "Admin? Go to Admin" link (not useful on login page)
</action>

<acceptance_criteria>
- Link to /register exists
- Clear navigation for users
</acceptance_criteria>

### Task 7: Add Register Route

<read_first>
- frontend/src/App.tsx
</read_first>

<action>
Add /register route:
- Import Register component
- Add Route for /register
- No authentication required (public)
</action>

<acceptance_criteria>
- /register route exists
- Accessible without login
</acceptance_criteria>

## Must-Haves

- [ ] POST /auth/register endpoint works
- [ ] Password validation (min 8 chars)
- [ ] Duplicate email detection
- [ ] Auto-login after registration
- [ ] Register UI page
- [ ] Navigation between login and register
