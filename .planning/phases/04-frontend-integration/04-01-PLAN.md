---
phase: 04-frontend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/
  - README.md
autonomous: true
requirements:
  - API-03
must_haves:
  truths:
    - Frontend has separate logged-in user routes and admin-only management routes
    - API client handles auth state consistently across user and admin pages
    - A complete admin-to-user flow works end to end
    - End-to-end verification covers login, admin upload, catalog display, authorized playback, and logout
    - README explains how to run frontend and backend locally
  artifacts:
    - path: frontend/
      provides: React frontend application with user and admin routes
      contains:
        - src/App.tsx
        - src/routes/
        - src/api/
        - src/auth/
    - path: README.md
      provides: Setup and run instructions
---

<objective>
Create React frontend with user catalog/playback routes, admin management routes, and connect to backend APIs. Verify end-to-end flow and document setup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-frontend-integration/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Initialize React frontend with Vite</name>
<files>frontend/</files>
<read_first>
- backend/app/main.py (for CORS config)
</read_first>
<behavior>
- Frontend runs on http://localhost:5173
- Can proxy API requests to backend
- Hot module replacement works
</behavior>
<action>
1. Create frontend directory and initialize Vite React TypeScript project:
   ```bash
   cd E:\code_github\cc-video
   npm create vite@latest frontend -- --template react-ts
   cd frontend
   npm install
   npm install react-router-dom
   ```

2. Update vite.config.ts for API proxy:
   ```typescript
   export default defineConfig({
     plugins: [react()],
     server: {
       proxy: {
         '/api': {
           target: 'http://localhost:8000',
           changeOrigin: true,
         }
       }
     }
   })
   ```
</action>
<verify>
<automated>ls frontend/package.json && grep "react-router-dom" frontend/package.json</automated>
</verify>
<done>
- Frontend directory exists with React + TypeScript + Vite
- React Router installed
- Vite proxy configured for /api
</done>
</task>

<task type="auto">
<name>Task 2: Create auth context and API client</name>
<files>frontend/src/auth/, frontend/src/api/</files>
<read_first>
- backend/app/routes/auth.py
- backend/app/schemas/token.py
</read_first>
<behavior>
- Auth context provides user state and login/logout functions
- API client adds auth header automatically
- Token stored in localStorage
</behavior>
<action>
1. Create frontend/src/auth/AuthContext.tsx:
   - AuthContext with user, login, logout, isLoading
   - Check token on mount via GET /api/v1/auth/me
   - Login calls POST /api/v1/auth/login
   - Logout clears localStorage

2. Create frontend/src/api/client.ts:
   - fetch wrapper with base URL
   - Auto-add Authorization header from localStorage
   - Handle 401 by clearing token

3. Create frontend/src/api/auth.ts:
   - login(username, password) → token
   - logout() → void
   - getCurrentUser() → user object
</action>
<verify>
<automated>ls frontend/src/auth/AuthContext.tsx frontend/src/api/client.ts frontend/src/api/auth.ts</automated>
</verify>
<done>
- AuthContext provides auth state
- API client handles auth headers
- Token persistence in localStorage
</done>
</task>

<task type="auto">
<name>Task 3: Create login page</name>
<files>frontend/src/routes/Login.tsx</files>
<read_first>
- frontend/src/auth/AuthContext.tsx
</read_first>
<behavior>
- Shows username/password form
- Redirects to /movies after successful login
- Shows error on failed login
</behavior>
<action>
Create frontend/src/routes/Login.tsx:
- Form with username and password inputs
- On submit, call login() from AuthContext
- On success, navigate to /movies
- On error, show error message
- Link to /admin-login for admin users (same form, different route)
</action>
<verify>
<automated>grep -E "login|password|submit" frontend/src/routes/Login.tsx</automated>
</verify>
<done>
- Login page with form
- Error handling
- Redirect after login
</done>
</task>

<task type="auto">
<name>Task 4: Create user catalog page</name>
<files>frontend/src/routes/Catalog.tsx, frontend/src/api/movies.ts</files>
<read_first>
- backend/app/routes/user.py
- backend/app/schemas/movie.py
</read_first>
<behavior>
- Shows list of published movies
- Each movie shows title, description
- Click navigates to playback page
- Requires auth
</behavior>
<action>
1. Create frontend/src/api/movies.ts:
   - getMovies() → list of movies
   - getMovie(id) → single movie

2. Create frontend/src/routes/Catalog.tsx:
   - Fetch movies on mount
   - Display in grid layout
   - Each card links to /movies/{id}
   - Show loading state
</action>
<verify>
<automated>ls frontend/src/routes/Catalog.tsx frontend/src/api/movies.ts</automated>
</verify>
<done>
- Catalog page shows movies
- Links to playback page
- Loading state handled
</done>
</task>

<task type="auto">
<name>Task 5: Create playback page</name>
<files>frontend/src/routes/Playback.tsx</files>
<read_first>
- backend/app/routes/user.py
- frontend/src/api/movies.ts
</read_first>
<behavior>
- Shows movie info (title, description)
- Video player with streaming
- Seeking works
- Requires auth
</behavior>
<action>
Create frontend/src/routes/Playback.tsx:
- Get movie ID from URL params
- Fetch movie details
- Render video element with src="/api/v1/movies/{id}/stream"
- Include Authorization header for video (may need special handling)
- Show movie title and description
- Handle non-existent movie
</action>
<verify>
<automated>grep -E "video|stream" frontend/src/routes/Playback.tsx</automated>
</verify>
<done>
- Playback page with video player
- Movie info displayed
- Streaming works
</done>
</task>

<task type="auto">
<name>Task 6: Create admin movie list page</name>
<files>frontend/src/routes/admin/Movies.tsx, frontend/src/api/admin.ts</files>
<read_first>
- backend/app/routes/admin.py
- backend/app/schemas/movie.py
</read_first>
<behavior>
- Shows all movies (all statuses)
- Actions: Create, Edit, Delete, Upload Video
- Requires admin role
</behavior>
<action>
1. Create frontend/src/api/admin.ts:
   - getAdminMovies() → all movies
   - createMovie(data) → new movie
   - updateMovie(id, data) → updated movie
   - deleteMovie(id) → void
   - uploadVideo(id, file) → void

2. Create frontend/src/routes/admin/Movies.tsx:
   - Table with movies
   - Status badge
   - Action buttons
   - Link to create new movie
</action>
<verify>
<automated>ls frontend/src/routes/admin/Movies.tsx frontend/src/api/admin.ts</automated>
</verify>
<done>
- Admin movies list
- CRUD actions available
- Status visible
</done>
</task>

<task type="auto">
<name>Task 7: Create admin movie edit page</name>
<files>frontend/src/routes/admin/EditMovie.tsx</files>
<read_first>
- backend/app/routes/admin.py
- backend/app/schemas/movie.py
</read_first>
<behavior>
- Form to edit title, description, status
- Video upload with progress
- Delete action
</behavior>
<action>
Create frontend/src/routes/admin/EditMovie.tsx:
- Form with title, description, publication_status
- Video file upload with progress indicator
- Save button calls updateMovie
- Delete button calls deleteMovie
- Handle loading and error states
</action>
<verify>
<automated>ls frontend/src/routes/admin/EditMovie.tsx</automated>
</verify>
<done>
- Edit movie form
- Video upload with progress
- Save and delete actions
</done>
</task>

<task type="auto">
<name>Task 8: Create admin create movie page</name>
<files>frontend/src/routes/admin/CreateMovie.tsx</files>
<read_first>
- frontend/src/routes/admin/EditMovie.tsx
- frontend/src/api/admin.ts
</read_first>
<behavior>
- Form to create new movie
- Redirects to edit page after creation
</behavior>
<action>
Create frontend/src/routes/admin/CreateMovie.tsx:
- Form with title, description, publication_status
- Default status: DRAFT
- Save button calls createMovie
- Redirect to /admin/movies/{newId} after creation
</action>
<verify>
<automated>ls frontend/src/routes/admin/CreateMovie.tsx</automated>
</verify>
<done>
- Create movie form
- Redirect after creation
</done>
</task>

<task type="auto">
<name>Task 9: Setup routing with auth protection</name>
<files>frontend/src/App.tsx, frontend/src/routes/ProtectedRoute.tsx</files>
<read_first>
- frontend/src/auth/AuthContext.tsx
</read_first>
<behavior>
- User routes: /login, /movies, /movies/:id
- Admin routes: /admin/movies, /admin/movies/new, /admin/movies/:id
- Redirect to /login if not authenticated
- Redirect to /movies if not admin for admin routes
</behavior>
<action>
1. Create frontend/src/routes/ProtectedRoute.tsx:
   - Check if user is authenticated
   - Check if user is admin (for admin routes)
   - Redirect to /login or /movies as needed

2. Update frontend/src/App.tsx:
   - BrowserRouter with Routes
   - Public: /login
   - Protected: /movies, /movies/:id
   - Admin: /admin/movies, /admin/movies/new, /admin/movies/:id
   - Default redirect / to /movies
</action>
<verify>
<automated>grep -E "BrowserRouter|Routes|Route" frontend/src/App.tsx</automated>
</verify>
<done>
- All routes configured
- Auth protection works
- Admin-only routes protected
</done>
</task>

<task type="auto">
<name>Task 10: Update backend CORS for frontend</name>
<files>backend/app/main.py</files>
<read_first>
- backend/app/main.py
- backend/app/config.py
</read_first>
<behavior>
- Backend accepts requests from http://localhost:5173
- Credentials allowed for cookies/headers
</behavior>
<action>
Update backend/app/main.py to add CORS middleware:
```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```
</action>
<verify>
<automated>grep -E "CORSMiddleware|allow_origins" backend/app/main.py</automated>
</verify>
<done>
- CORS configured for frontend
- Credentials allowed
</done>
</task>

<task type="auto">
<name>Task 11: Create README documentation</name>
<files>README.md</files>
<read_first>
- backend/app/config.py
- backend/requirements.txt
- frontend/package.json
</read_first>
<behavior>
- Setup instructions for backend
- Setup instructions for frontend
- How to run both
- API overview
</behavior>
<action>
Create comprehensive README.md:
1. Project overview
2. Prerequisites (Python 3.11+, Node.js 18+)
3. Backend setup:
   - cd backend
   - python -m venv venv
   - source venv/bin/activate (or venv\Scripts\activate on Windows)
   - pip install -r requirements.txt
   - Set environment variables (DATABASE_URL, SECRET_KEY)
   - alembic upgrade head
   - uvicorn app.main:app --reload
4. Frontend setup:
   - cd frontend
   - npm install
   - npm run dev
5. Default admin account
6. API endpoints summary
</action>
<verify>
<automated>grep -E "npm run dev|uvicorn|setup" README.md</automated>
</verify>
<done>
- README with full setup instructions
- Backend and frontend docs
- API summary
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Backend API | Untrusted input; all endpoints require auth |
| Browser localStorage | Token storage; XSS risk mitigated by HttpOnly cookies in future |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Frontend login | mitigate | Login requires valid credentials, returns JWT |
| T-04-02 | Information Disclosure | Token storage | accept | localStorage used for v1 simplicity; HttpOnly cookies in future |
| T-04-03 | Tampering | API requests | mitigate | JWT signature validation on backend |

</threat_model>

<verification>
## Automated Verification

1. Frontend builds: `cd frontend && npm run build`
2. Frontend runs: `cd frontend && npm run dev`
3. All routes accessible: manual verification
4. CORS works: frontend can call backend
</verification>

<success_criteria>
- [ ] Frontend has separate user and admin routes
- [ ] API client handles auth state consistently
- [ ] End-to-end flow works: admin upload -> user catalog -> user playback
- [ ] README explains how to run both frontend and backend
- [ ] All protected routes require authentication
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-integration/04-01-SUMMARY.md`
</output>
