---
wave: 1
depends_on: []
files_modified:
  - backend/app/models/group.py
  - backend/app/services/group_service.py
  - backend/app/routes/group.py
  - frontend/src/api/group.ts
requirements_addressed:
  - UGC-01
  - UGC-02
  - UGC-03
  - UGC-04
  - UGC-05
autonomous: true
---

# Plan: User Groups Backend Implementation

<objective>
Implement user groups with membership management, collections, discussions, and activity feeds.
</objective>

<read_first>
- backend/app/models/forum.py — Model patterns
- backend/app/services/forum_service.py — Service patterns
- backend/app/routes/forum.py — Route patterns
</read_first>

## Tasks

### Task 1: Create Group Models

<action>
Create backend/app/models/group.py with GroupPrivacy enum, GroupRole enum, and models:
- Group: id, tenant_id, name, slug, description, privacy, cover_image, icon, created_by, member_count, is_active, created_at, updated_at
- GroupMember: id, group_id, user_id, tenant_id, role, joined_at
- GroupCollection: id, group_id, tenant_id, name, description, created_by, item_count, created_at
- GroupCollectionItem: id, collection_id, movie_id, added_by, added_at
- GroupDiscussion: id, group_id, author_id, tenant_id, title, content, is_pinned, is_locked, reply_count, created_at, updated_at
- GroupDiscussionReply: id, discussion_id, author_id, tenant_id, content, created_at
- GroupActivity: id, group_id, user_id, tenant_id, activity_type, content, created_at

Follow patterns from backend/app/models/forum.py.
</action>

<acceptance_criteria>
- backend/app/models/group.py exists
- File contains GroupPrivacy enum with PUBLIC, PRIVATE values
- File contains GroupRole enum with OWNER, ADMIN, MEMBER values
- File contains all 7 models with correct fields
- All models inherit from Base
- All foreign keys reference correct tables
</acceptance_criteria>

### Task 2: Create Group Service

<action>
Create backend/app/services/group_service.py with GroupService class implementing:
- create_group, get_groups, get_group, update_group, delete_group
- join_group, leave_group, invite_member, get_members, update_member_role, remove_member
- create_collection, get_collections, add_collection_item, remove_collection_item
- create_discussion, get_discussions, create_discussion_reply
- get_activity_feed, log_activity

Follow patterns from backend/app/services/forum_service.py.
</action>

<acceptance_criteria>
- backend/app/services/group_service.py exists
- File contains GroupService class with all 20 methods
- Methods use async/await pattern
- create_group adds creator as owner member
- Activity logging works for all activity types
</acceptance_criteria>

### Task 3: Create Group Routes

<action>
Create backend/app/routes/group.py with FastAPI router implementing all endpoints:
- CRUD for groups
- Membership management (join, leave, invite, update role, remove)
- Collections CRUD
- Discussions and replies
- Activity feed

Follow patterns from backend/app/routes/forum.py.
</action>

<acceptance_criteria>
- backend/app/routes/group.py exists
- File contains router with prefix /groups
- All 19 endpoints defined with async def
- Permission checks for admin/owner-only endpoints
- HTTPException raised with proper status codes
</acceptance_criteria>

### Task 4: Create Frontend API Client

<action>
Create frontend/src/api/group.ts with interfaces and API functions for all group operations.
Follow patterns from frontend/src/api/forum.ts.
</action>

<acceptance_criteria>
- frontend/src/api/group.ts exists
- File exports all interfaces and API functions
- Functions use apiClient for HTTP requests
</acceptance_criteria>

## Verification

- Run: cd backend && python -c "from app.models.group import Group; print OK"
- Run: cd backend && python -c "from app.services.group_service import GroupService; print OK"
- Run: cd backend && python -c "from app.routes.group import router; print OK"
- Run: cd frontend && npx tsc --noEmit src/api/group.ts

## must_haves

- Groups can be created with public/private visibility
- Users can join/leave groups
- Group owners can manage members
- Groups have collections containing movies
- Groups have discussion boards with replies
- Activity feed shows group events
