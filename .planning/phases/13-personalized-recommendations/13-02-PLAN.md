---
wave: 2
depends_on:
  - 13-01
files_modified:
  - frontend/src/api/recommendations.ts
  - frontend/src/components/Recommendations.tsx
  - frontend/src/components/ContinueWatching.tsx
  - frontend/src/routes/Catalog.tsx
requirements_addressed:
  - REC-01
  - REC-02
autonomous: true
---

# Phase 13-02: Recommendations Frontend

<objective>
Create frontend components to display personalized recommendations and continue watching sections on the catalog page.
</objective>

<read_first>
- frontend/src/api/movies.ts — Existing API patterns
- frontend/src/routes/Catalog.tsx — Current catalog page
- frontend/src/components/ — Existing component patterns
- frontend/src/api/types.ts — Type definitions
</read_first>

<tasks>

<task id="1" type="execute">
<name>Create recommendations API client</name>
<action>
Create `frontend/src/api/recommendations.ts` with:
- `getRecommendations()` — Fetch personalized recommendations
- TypeScript types matching backend response
- Use existing fetchApi pattern from auth.ts
</action>
<acceptance_criteria>
- `frontend/src/api/recommendations.ts` exists
- Exports getRecommendations function
- Types match backend response structure
</acceptance_criteria>
</task>

<task id="2" type="execute">
<name>Create ContinueWatching component</name>
<action>
Create `frontend/src/components/ContinueWatching.tsx` with:
- Horizontal scrollable list of movies
- Progress bar showing watch progress
- Click navigates to playback with resume position
- Title "Continue Watching"
- Empty state if no items
</action>
<acceptance_criteria>
- `frontend/src/components/ContinueWatching.tsx` exists
- Shows progress bar for each item
- Clicking item navigates to playback
</acceptance_criteria>
</task>

<task id="3" type="execute">
<name>Create Recommendations component</name>
<action>
Create `frontend/src/components/Recommendations.tsx` with:
- Horizontal scrollable list of movies
- Shows recommendation reason (e.g., "Because you watched...")
- Click navigates to movie details
- Title "Recommended for You"
- Empty state if no items
</action>
<acceptance_criteria>
- `frontend/src/components/Recommendations.tsx` exists
- Shows recommendation reason
- Clicking item navigates to movie
</acceptance_criteria>
</task>

<task id="4" type="execute">
<name>Update Catalog page with recommendations</name>
<action>
Update `frontend/src/routes/Catalog.tsx`:
- Fetch recommendations on mount (for logged-in users)
- Add ContinueWatching section at top (if items exist)
- Add Recommendations section below (if items exist)
- Keep existing movie grid below recommendations
- Handle loading and error states
</action>
<acceptance_criteria>
- Catalog shows continue watching section
- Catalog shows recommendations section
- Sections only appear if data available
- Existing movie grid still works
</acceptance_criteria>
</task>

</tasks>

<verification>
- Frontend builds without TypeScript errors
- Recommendations appear on catalog page
- Continue watching shows progress
- Clicking items navigates correctly
- Empty states display appropriately
</verification>

<must_haves>
- Recommendations visible on catalog page
- Continue watching with progress indication
- Proper navigation to playback
- Responsive layout
</must_haves>
